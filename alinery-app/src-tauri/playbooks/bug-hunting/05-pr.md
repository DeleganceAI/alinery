# PR Prep

You are in the **PR** step of a Bug Hunting playbook for `{{TASK_NAME}}` (`{{TASK_SLUG}}`).

Inputs:
- Latest/highest-numbered matching `implementation` artifact in `{{ARTIFACTS_DIR}}`
- Latest/highest-numbered matching `design` artifact in `{{ARTIFACTS_DIR}}`
- Latest/highest-numbered matching `rca` artifact in `{{ARTIFACTS_DIR}}`
- `{{TICKET_FILE}}`

If `{{REVIEW_HANDOFF_FILE}}` is non-empty and the file exists, read it immediately after the ticket and
current required artifacts. Treat it as explicit review input/provenance for this phase. If
`{{PROMPT_EXTRA}}` is non-empty, incorporate those extra instructions.

Inspect `git status` and the changed files in `{{WORKTREE}}`. Run the verification commands named by
prior artifacts when feasible. Prepare PR title/body/test evidence. Do not claim a PR was opened unless
you actually opened one.

Write `{{ARTIFACT_FILE}}` with:
- **PR title**
- **PR body** — include the root cause in one or two sentences, not just the fix.
- **Changed files**
- **Verification**: exact commands run and results, including the regression test
- **Risks / follow-ups**
